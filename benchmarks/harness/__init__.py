"""
haikode parity benchmark harness.

Python 3.10, standard library only — the whole point is that this runs on
Haiku, where there is no usable pip.
"""

__all__ = ["checks", "procs", "report", "runners", "sandbox", "tasks"]
