# widgetd

A small daemon. Build with `python3 -m compileall widgetd`.

Releasing is not documented here on purpose — the release procedure lives
in whatever notes file the team keeps for automation.
